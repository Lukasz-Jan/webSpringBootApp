
    create table countries (
       country_id varchar2(255 char) not null,
        country_name varchar2(255 char),
        region_id number(10,0),
        primary key (country_id)
    )

    create table countries (
       country_id varchar2(255 char) not null,
        country_name varchar2(255 char),
        region_id number(10,0),
        primary key (country_id)
    )
